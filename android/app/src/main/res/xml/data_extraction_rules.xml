<?xml version="1.0" encoding="utf-8"?>
<!--
  Block cloud backup AND device-to-device transfer of every byte the app
  writes. Encrypted media must never leave the device via backup channels:
  Keystore keys are non-exportable, so restored ciphertext would be
  undecryptable anyway — but we refuse to leak ciphertext or metadata at all.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
